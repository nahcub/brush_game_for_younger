# plan.md — 페이스메시 양치 게임 프로토타입

## 이 문서의 목적
노트북 웹캠으로 동작하는 **양치 게임 프로토타입**을 만든다.
목표는 단 하나: **"페이스메시로 아이가 양치 중인지 감지하고, 그걸 게임으로 재밌게 만들 수 있는가"를 검증**하는 것.
과하게 만들지 말 것. 핵심 루프가 웹캠에서 실제로 도는지 눈으로 확인하는 게 1차 목표다.

최종 타겟은 태블릿이지만, 지금은 노트북 웹캠에서 개발한다. 웹 기술로 만들면 태블릿 이식이 거의 그대로 된다.

---

## 기술 스택
- **빌드**: Vite + 바닐라 JS (프레임워크 없이 시작. 상태가 복잡해지면 나중에 React로 이관)
- **얼굴 인식**: MediaPipe Tasks Vision `FaceLandmarker` (`@mediapipe/tasks-vision`)
  - 468개 얼굴 랜드마크를 실시간 제공
  - **온디바이스(브라우저 내) 실행** — 영상이 서버로 안 나감. 이건 이 제품의 핵심 세일즈 포인트이므로 반드시 유지
- **웹캠**: 브라우저 `navigator.mediaDevices.getUserMedia`
- **렌더링**: Canvas 2D (필터·타겟·게이지·반짝이 등). 3D는 지금 불필요, 쓰지 말 것
- **스타일**: 최소한의 CSS. 예쁘게 만드는 건 나중. 지금은 기능 검증 우선

### 하지 말 것
- Python/OpenCV 데스크톱 앱 (태블릿 이식 불가)
- Three.js, 무거운 3D 필터 (오버스펙)
- 서버 백엔드, DB, 로그인 (프로토타입에 불필요. 전부 브라우저 안에서)
- localStorage 등 영속 저장은 지금 안 해도 됨 (세션 내 메모리 상태로 충분)

---

## 판정 방식 (중요 — 여기가 핵심 설계)

**정밀하게 "어느 이를 닦는지"는 판정하지 않는다.** 오직 **"지금 양치 중인가 / 아닌가"** 이진 판정만 한다.

### 양치 중 판정 로직 (단순하게)
"입 주변에서 칫솔질에 해당하는 반복적 움직임이 있는가"를 감지한다. 페이스메시로 다음을 활용:

1. **얼굴이 인식되고 있는가** (카메라 앞에 사람이 있는가)
2. **입 주변 랜드마크의 움직임** — 입 벌림(위/아래 입술 랜드마크 거리 변화), 턱 움직임
3. **입 영역 앞의 손/칫솔 움직임** — 이건 페이스메시만으론 직접 안 잡힘. 대안:
   - 입 주변 랜드마크가 손/칫솔에 **가려지거나(occlusion)**, 얼굴 하관 움직임이 **주기적으로 반복**되면 "양치 중"으로 판정
   - MVP에서는 **"입 벌린 상태 + 하관/턱의 반복적 움직임이 감지되면 양치 중"** 정도로 단순하게 시작

### 판정을 너무 빡세게 만들지 말 것
- 목적은 "감시"가 아니라 "게임을 진행시키는 트리거"다.
- 애매하면 관대하게(양치 중으로) 판정. 아이가 억울하게 멈추는 것보다 낫다.
- 움직임이 일정 시간(예: 2~3초) 없으면 "양치 멈춤"으로 보고 게임 진행 일시정지.

### 개발 중 튜닝
- 움직임 감지 임계값은 하드코딩된 상수로 두고, 화면에 디버그 값(현재 움직임 수치, 판정 상태)을 표시해서 웹캠 테스트하며 조정할 수 있게 할 것.

---

## 개발 순서 (단계별로. 한 번에 다 하지 말 것)

각 단계가 동작하는 걸 확인한 뒤 다음으로 넘어간다.

### 1단계: 웹캠 + 얼굴 인식 시각화 (가장 먼저)
- 웹캠 스트림을 화면에 표시
- FaceLandmarker로 얼굴 인식, 468개 랜드마크를 Canvas에 점으로 오버레이
- 화면에 "얼굴 인식됨 / 안 됨" 상태 표시
- **목표: 페이스메시가 노트북 웹캠에서 실제로 도는지 확인**

### 2단계: "양치 중" 판정
- 위 판정 로직 구현
- 화면에 실시간으로 "양치 중 / 멈춤" 상태 + 디버그 수치 표시
- **목표: 실제로 칫솔질할 때 "양치 중"으로 잡히는지 웹캠으로 검증**

### 3단계: 입 주변 필터/타겟 (재미 요소)
- 입 주변 랜드마크 위치에 간단한 필터 오버레이 (예: 반짝이는 거품, 별, 캐릭터 입 장식)
- 양치 중일 때 필터가 반응(움직이거나 반짝임)
- **목표: 아이가 "재밌다"고 느낄 최소한의 시각 피드백**

### 4단계: 진행 게이지 + 타이머 + 완료
- 2분(테스트 중엔 짧게, 예: 20초) 타이머
- 양치 중일 때만 진행 게이지가 참 (멈추면 게이지도 멈춤)
- 게이지 다 차면 "양치 완료!" 연출 (팡파레, 화면 효과)
- **목표: 핵심 루프의 "완료" 지점까지 도달**

### 5단계: 상자 획득 연출
- 완료 시 **상자를 획득**하되, **잠긴 상태**로 표시
- "다음 양치 때 열려요!" 메시지
- (상자 개봉·마을은 이번 프로토타입 범위 밖 — 자리표시자로만)

---

## 상자 시스템 설계 (구현은 4~5단계, 로직 참고용)

핵심 규칙 (클래시 로열 응용, 단 시간이 아니라 **다음 양치**에 연동):
- 양치 완료 → 상자 **획득(잠김)**
- **다음 양치를 시작할 때** 이전 상자가 **열림**
- 즉: 양치 시작 = 어제 상자 개봉(동기 부여), 양치 완료 = 오늘 상자 획득(보상)

상자 등급 (양치 품질·지속성에 연동 — 순수 랜덤 아님):
- **실버박스**: 기본 양치 완료 (매번 보장)
- **골드박스**: 2분 꽉 채움 / 중간에 안 멈춤
- **슈퍼마법박스**: 연속 며칠 성공(스트릭) 또는 하루 2회 다 함

> 이번 프로토타입에서는 상자 "획득 + 잠김" 표시까지만. 개봉·등급별 내용물·마을은 다음 버전.

### 도박성 배제 원칙 (설계에 반영, 어기지 말 것)
- 상자에 **꽝 없음** (항상 마을 재료가 나옴, "얼마나 좋은가"의 차이만)
- 상자를 **돈으로 살 수 없음** (오직 양치로만 획득) — "brush to earn", "pay to win" 아님
- 이 원칙은 아동 대상 서비스의 규제·윤리상 필수

---

## 화면 구성 (MVP)
```
+-------------------------------------------+
|  [웹캠 영상 + 얼굴 랜드마크 + 입 필터]      |
|                                           |
|         (양치 중 필터 반응)                 |
|                                           |
+-------------------------------------------+
|  진행 게이지 [==========-----]  0:45 / 2:00 |
|  상태: 양치 중                              |
|  [디버그] 움직임: 0.23  판정: BRUSHING      |
+-------------------------------------------+
```
- 웹캠 영상이 화면 대부분
- 하단에 게이지·타이머·상태·디버그 정보
- 완료 시 오버레이로 "완료!" + 상자 연출

---

## 알려진 기술 리스크 (미리 인지)
- **칫솔 위치 정밀 추적은 페이스메시만으론 불가** → 그래서 "양치 중/아님" 이진 판정으로 단순화한 것. 이 방향이 맞음.
- **움직임 감지가 오탐할 수 있음** (말하기, 웃기 등도 하관이 움직임) → 임계값 튜닝 + "일정 시간 지속된 반복 움직임"으로 필터링. 디버그 표시로 조정.
- **조명·웹캠 품질**에 인식률이 좌우됨 → 밝은 환경에서 테스트. 태블릿 카메라는 대체로 웹캠보다 나음.

---

## 완료 기준 (이 프로토타입이 성공인가)
1. 노트북 웹캠에서 얼굴·랜드마크가 안정적으로 인식된다
2. 실제로 칫솔질하면 "양치 중"으로 잡히고, 멈추면 "멈춤"으로 잡힌다
3. 양치 중일 때 필터가 반응해서 "재밌다"는 느낌이 최소한 든다
4. 게이지가 차고 완료 연출 + 상자 획득까지 도달한다

이 4개가 되면 검증 성공. 그 다음에 상자 개봉·마을·스트릭·디자인을 붙인다.

---
